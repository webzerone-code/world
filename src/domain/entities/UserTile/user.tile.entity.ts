import { UserTileType } from './user.tile.type';

export class UserTileEntity {
  private id: string;
  private userId: string;
  private xPosition: number;
  private yPosition: number;
  private tileType: UserTileType;
}
